import { FjNativeDropdownModule } from './native-dropdown.module';

describe('NativeDropdownModule', () => {
  let nativeDropdownModule: FjNativeDropdownModule;

  beforeEach(() => {
    nativeDropdownModule = new FjNativeDropdownModule();
  });
});
